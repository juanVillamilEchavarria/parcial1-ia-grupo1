services:
    app:
        build: 
            context: .
            dockerfile: Dockerfile
        ports:
            - "5000:5000"
        volumes:
            - ".:/app"

networks:
    default:
        driver: bridge